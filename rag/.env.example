# rag/.env.example
# 이 파일을 rag/.env로 복사한 뒤 실제 값을 채운다 (.env는 커밋 금지)

# ---------------- NCP pgvector Vector DB ----------------
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=

# ---------------- LLM (별도 llm 서버의 REST API) ----------------
LLM_API_URL=http://127.0.0.1:8000/llm/replies
